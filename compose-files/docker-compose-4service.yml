version: '3'

services:

  client:
    image: nginx
    ports:
      - "8082:80"

  api:
    build: .

  cache:
    image: redis

  database:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: appdb

volumes:
  db_data:
